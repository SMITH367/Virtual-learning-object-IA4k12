---
// Si necesitas alguna prop aquí, la puedes definir
---

<nav class="bg-indigo-800 text-white p-4 shadow-md sticky top-0 z-50"> {/* Añadimos z-50 para asegurar que el nav siempre esté por encima del contenido */}
  <div class="container mx-auto flex justify-between items-center">
    <a href="/" class="text-2xl font-bold">Mentes Futuras IA</a>

    {/* Botón de Hamburguesa para Móviles */}
    <button id="menu-button" class="md:hidden text-white focus:outline-none">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path id="menu-icon-open" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
        <path id="menu-icon-close" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>

    {/* Menú de Navegación */}
    <ul id="main-menu" class="hidden md:flex flex-col md:flex-row space-y-4 md:space-y-0 md:space-x-6 absolute md:relative top-full left-0 w-full md:w-auto bg-indigo-800 md:bg-transparent p-4 md:p-0 shadow-lg md:shadow-none">
      <li>
        <a
          href="/#about"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Sobre el Proyecto</a
        >
      </li>
      <li>
        <a
          href="/cursos"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Cursos</a
        >
      </li>
      <li>
        <a
          href="/#methodology"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Metodología</a
        >
      </li>
      <li>
        <a
          href="/#advantages"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Beneficios Clave</a
        >
      </li>
      <li>
        <a
          href="/#testimonials"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Testimonios</a
        >
      </li>
      <li>
        <a
          href="/#partners"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Nuestros Aliados</a
        >
      </li>
      <li>
        <a
          href="/#contact"
          class="block py-2 hover:text-green-300 transition-colors duration-200"
          >Contacto</a
        >
      </li>
    </ul>
  </div>
</nav>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const menuButton = document.getElementById('menu-button');
    const mainMenu = document.getElementById('main-menu');
    const menuIconOpen = document.getElementById('menu-icon-open');
    const menuIconClose = document.getElementById('menu-icon-close');

    if (menuButton && mainMenu && menuIconOpen && menuIconClose) {
      menuButton.addEventListener('click', () => {
        mainMenu.classList.toggle('hidden'); // Alterna la visibilidad del menú
        menuIconOpen.classList.toggle('hidden'); // Alterna el icono de hamburguesa
        menuIconClose.classList.toggle('hidden'); // Alterna el icono de cerrar
      });

      // Opcional: Cerrar el menú cuando se hace clic en un enlace (útil para Single Page Apps)
      mainMenu.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
          if (!mainMenu.classList.contains('hidden')) { // Solo si el menú está abierto
            mainMenu.classList.add('hidden');
            menuIconOpen.classList.remove('hidden');
            menuIconClose.classList.add('hidden');
          }
        });
      });

      // Opcional: Cerrar el menú si la ventana cambia de tamaño a desktop
      window.addEventListener('resize', () => {
        if (window.innerWidth >= 768) { // 768px es el breakpoint 'md' por defecto de Tailwind
          mainMenu.classList.remove('hidden');
          menuIconOpen.classList.add('hidden');
          menuIconClose.classList.add('hidden');
        } else {
          // Si estamos en móvil y el menú no está oculto, asegúrate de ocultarlo al redimensionar a móvil
          if (!mainMenu.classList.contains('hidden')) {
              mainMenu.classList.add('hidden');
              menuIconOpen.classList.remove('hidden');
              menuIconClose.classList.add('hidden');
          }
        }
      });
    }
  });
</script>

<style is:global>
  /*
    Asegúrate de que @tailwind base, components y utilities
    estén importados globalmente en tu archivo CSS principal
    o en un bloque <style is:global> en un layout o el archivo raíz.
  */
</style>