---
import IaMachineLearning from '../../../components/games/MachineLearning.jsx';
// 1. FUNCIÓN OBLIGATORIA: Define las rutas para los 4 grados
export async function getStaticPaths() {
    return [
        { params: { grado: 'primero' } },
        { params: { grado: 'segundo' } },
        { params: { grado: 'tercero' } },
        { params: { grado: 'cuarto' } },
    ];
}

// 2. OBTENER EL PARÁMETRO DE LA URL.
const { grado } = Astro.params; 

// 3. Pasar el parámetro al componente de React.
const gradoProps = { grado: grado };
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OVA ML - Grado {gradoProps.grado.replace('grado', '')}°</title>
</head>
<body class="bg-gray-50 min-h-screen">
    <main class="max-w-4xl mx-auto p-4 sm:p-8">
        <header>
            <h1 class="text-4xl font-extrabold text-center mb-6 text-gray-800">
                🧠 La Esencia de Machine Learning
            </h1>
            
            
        </header>

        <IaMachineLearning 
            grado={gradoProps.grado}
            client:load 
        />
        
        <footer class="mt-8 text-center text-gray-500 text-sm">
            <p>Hecho para enseñar: La IA es un cerebro que aprende con datos.</p>
        </footer>
    </main>
</body>
</html>